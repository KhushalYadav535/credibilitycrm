import { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Voice Bot Use Cases | Customer Engagement Automation | Credibility",
  description: "Discover how Credibility's AI voice platform automates customer interactions across six core use cases — from grievance redressal to lead capture — deployable across any industry.",
};

export default function UseCasesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
